import { IConfirmFormikProps } from "@/hooks/forms/useConfirmForm/useConfirmForm.types";
import { FormikProps } from "formik";

export interface IAgentTypeProps {
  formik: FormikProps<IConfirmFormikProps>
}
